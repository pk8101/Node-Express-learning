{
  "name": "httpserver",
  "version": "1.0.0",
  "description": "creating server pratice",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pradeep",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0"
  }
}
